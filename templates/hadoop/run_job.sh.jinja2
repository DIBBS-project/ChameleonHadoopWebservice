#!/bin/bash

sudo adduser {{ user }} || true
sudo usermod -a -G supergroup {{ user }} || true

sudo -u {{ user }} bash -c "source /etc/environment; $HADOOP_HOME/bin/hadoop jar tmp/{{ command }} {{ suffix }}"
