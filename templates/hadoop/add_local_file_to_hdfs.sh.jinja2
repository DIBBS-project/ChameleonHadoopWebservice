#!/bin/bash

sudo adduser {{ user }} || true
sudo usermod -a -G supergroup {{ user }} || true

sudo -u {{ user }} bash -c "source /etc/environment; $HADOOP_HOME/bin/hadoop fs -put tmp/{{ local_path }} /{{ hdfs_path }}"
